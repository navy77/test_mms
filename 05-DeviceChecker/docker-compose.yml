services:
  devicechecker:
    build: .
    image: mic/devicechecker:1.0.0
    container_name: devicechecker
    restart: always
    env_file:
      - ../.env
    volumes:
      - ./log:/app/log
    networks:
      - mms-network

networks:
  mms-network:
    external: true
